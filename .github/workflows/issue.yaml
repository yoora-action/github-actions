name: issue-workflow
on:
  issues:
    types: [opened]

jobs:
  issue-job:
    runs-on: ubuntu-latest
    steps:
    - name: step1
      run: echo hello world
    - name: step2
    # 멀티라인 커맨드 작성
      run: |
        echo hello world
        echo github action